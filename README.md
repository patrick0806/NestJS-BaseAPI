# Reference API

# Project status: Completed (for now)

This is a skeleton API so we can start new projects.
Quickly focus only on implementing business rules

## Requirements

- **Node.js >= 24** (LTS). Enforced via the `engines` field in `package.json`.
- npm 10+ (ships with Node 24).

## How to run

This project uses [NestJS](https://docs.nestjs.com/).

So it is recommended that you know a little about this framework

To run this project in dev mode you need to follow these steps

### Install dependencies

```bash
 npm install
```

### Run project

```bash
 npm run start:dev
```

### API Reference (Scalar)

Once the app is running, the API reference UI is served by [Scalar](https://scalar.com) at:

```
 http://localhost:3000/api/v1/docs
```

The OpenAPI document is generated by `@nestjs/swagger` and rendered by `@scalar/nestjs-api-reference` (Fastify adapter).

### Run tests

```bash
 npm run test
```

### Lint

```bash
 npm run lint
```

## What we have already done in this skeleton

- SWC (fast compiler)
- Fastify
- CORS and Helmet
- Vitest
- Docker Configuration
- Prettier and ESLint (flat config) rules
- Validations
- Exception Handler
- Base recorder (Winston)
- API versioning
- Debug mode in VSCode
- All routes are protected by default
- Health Check with terminus
- Using Passport
- Role validation (Role decorator) // like @Roles(ApplicationRole.ADMIN) in controller
- GitHub Actions Pipeline for validating tests
- Scalar API Reference (replaces Swagger UI)

# Technologies

- [NodeJS](https://nodejs.org/en/docs)
- [NestJS](https://docs.nestjs.com/)
- [SWC](https://swc.rs/)
- [Vitest](https://vitest.dev/)
- [Scalar](https://scalar.com/) (API Reference)

# Project Structure

## The project code is all inside src folder and we have three principal folders config, modules, shared

### Config

Here we have config files, like the API reference or typeorm configs. The base structure is like

```bash
    /config
        /env
            index.ts
        /apiReference
            apiReference.config.ts
            defaultResponses.ts
        /typeorm
            /migrations
            datasource.ts
```

### Modules

Here we have our business logic and unit tests. Modules are contexts of a module and every module has the same structure like

```bash
  /modules
    /contexts
        /create
            /dtos
                request.dto.ts
                response.dto.ts
            /tests
                create.spec.ts
            create.controller.ts
            create.service.ts
    moduleName.module.ts
```

### Shared

We have the common logic of the application and shared things like entities and repositories that we can use in any module. The structure is like

```bash
    /shared
        /constants
            apiTags.ts
            index.ts
        /decorators
            public.decorator.ts
            roles.decorator.ts
            index.ts
        /dtos
            error.dto.ts
            index.ts
```
